import { CollectionOptions } from "@webscale/collection";

export const releases = "releases";

export const releaseCollectionOptions: CollectionOptions = {
  singularName: "release",
  readonly: false
};
